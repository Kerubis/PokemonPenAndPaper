services:
  app:
    image: 172.16.100.10:5000/pokemon-pen-and-paper:latest
    container_name: PokemonPenAndPaper
    restart: unless-stopped
    networks:
      br0:
        ipv4_address: 172.16.100.30
    env_file:
      - .env

networks:
  br0:
    external: true
    name: br0
